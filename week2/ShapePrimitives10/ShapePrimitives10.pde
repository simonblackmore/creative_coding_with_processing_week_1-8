//The two lines represent allow comments

/*
so does this
Super useful... This is where we declare variables
later add libraries to do more complex things.
*/

void setup(){
// The setup() function is called once when the program starts.
};

void draw(){
/*
Called directly after setup() and continuously executes 
the lines of code contained inside its block until the 
program is stopped or noLoop() is called.
*/
};

// We will add our own functions later here
